\subsection{Macaque motor cortex spike data in an active reaching task}
\label{subsection:churchland_results}

% Given the outline we discussed before:
% 1. Introduce the dataset and discovery workflow.
% 2. Show 2–4 interpretable features discovered with NLDisco.
% 3. Explain plots and metrics.
% 4. Briefly introduce CEBRA and LangevinFlow.
% 5. Compare recovery of the identified features and metrics across all methods, incl. decoding performance.

To test NLDisco on real neural data with unknown ground-truth representations, and to compare to other established neural LVM models, we analyze neural and behavioral data from a popular macaque motor control task~\citep{churchland_2012_population_dynamics}, prepared by Brainsets~\citep{azabou_2023_unified}. Briefly, the task requires the macaque to wait for a go cue before reaching to a visual target with a hand-controlled cursor while avoiding virtual barriers in different configurations (\autoref{figure:task_overview}b).

We analyze one experiment comprising 1,906 retained trials across 27 configurations with nine total target locations. Unsorted spikes were recorded from dorsal premotor and primary motor cortex using $N=191$ Utah array channels, and neural and behavioral data are binned at 50 ms. To try to find both instantaneous and temporally-evolving features, we use $S=10$ to capture 500 ms of activity per example, and train a two-level Matryoshka TW-SED with $(D_1,k_1)=(96,8)$, $(D_2,k_2)=(192,16)$, and $B=1024$. Inspection of latent-conditioned kinematics and task conditions guide feature visualization: behavioral descriptions are developed from trained latents, then fixed for comparison.

Among many features found (see \hyperref[subsubsection:churchland_results_additional]{\ref*{subsubsection:churchland_results_additional}}), here we highlight two that together span kinematics and task conditions: 1) \textit{recent deceleration}: a speed slope $\leq-75$ mm/s$^2$ over the preceding 250 ms; 2) \textit{fast leftward target reach}: a movement within $45^\circ$ of leftward at endpoint speed $\geq500$ mm/s on trials with target positioned at $(-144,-52)$ mm of screen center. We visualize them by plotting the probability of latent activation conditioned on behavioral and environmental variables (\autoref{fig:churchland_results}a), and quantify these associations using standard metrics for coverage (\metric{Cov}), specificity (\metric{Spec}), selectivity (\metric{Sel}), and discriminability (\metric{AUROC}; \autoref{fig:churchland_results}b,c). \metric{Cov} measures how often a latent activates within the condition, \metric{Spec} how often it remains inactive outside it, \metric{Sel} summarizes this preference, and \metric{AUROC} measures discrimination across activation thresholds (definitions in \hyperref[subsubsection:latents_evaluation_details]{\ref*{subsubsection:latents_evaluation_details}}). All metrics range from 0 to 1, with a higher value indicating a better score.

% We then quantify their selectivity and discriminability with two standard metrics: 1) the selectivity of latent activation for the feature-defining condition ($\metric{Sel}$); 2) the area under the receiver operating characteristic curve ($\metric{AUROC}$), which measures how well latent activity discriminates between windows satisfying the defining condition and those that do not (\autoref{fig:churchland_results}b; see \hyperref[subsubsection:latents_evaluation_details]{\ref*{subsubsection:latents_evaluation_details}} for mathematical definitions).

\begin{figure}[tb]
    \centering
    \includegraphics[width=\linewidth]{figures/figure4.pdf}
    \caption{\textbf{Interpreting and comparing features in 192-dimensional representations.} (\textbf{a}) NLDisco latent activation probability conditioned on speed slope over the preceding 250 ms (top, latent 83) or endpoint velocity direction and speed (bottom, latent 20), for the second level ($D=192$, $k=16$) of the two-level Matryoshka TW-SED trained on examples of $S=10$ with 50 ms bins. Both heatmaps pool data from all valid experiment trials; they do \textit{not} restrict observations to the scored conditions (gray cells have fewer than 10 distinct trials). (\textbf{b}) \metric{Cov} versus \metric{Spec} and (\textbf{c}) \metric{Sel} versus \metric{AUROC} for the same NLDisco, CEBRA-Time, and LangevinFlow latents that best match these features. Source activity traceback appears in \autoref{figure:churchland_traceback}.}
    \label{fig:churchland_results}

\end{figure}

We then compare results to other popular neural LVMs. For fair comparisons focused on the main goal of unsupervised interpretable latent discovery, we consider published methods (\autoref{table:method_comparisons}) that: 1) prioritize an interpretable latent space; 2) do not require multimodal or trial-structured data. Among these, we show results from CEBRA (which contrastively learns a continuous neural embedding space~\citep{schneider_2023_cebra}) and LangevinFlow (which models neural activity through structured, stochastic dynamics~\citep{song_2025_langevinflow}) as they are current top-performing contrastive and sequential variational methods, respectively, on Neural Latents Benchmark~\citep{pei_2022_nlb}.

We set the latent space for each of these baseline methods to 192 to match the SED $D$, and perform 12-run sweeps for each method, varying temperature and positive-pair lag for CEBRA-Time, and learning rate and regularization-ramp duration for LangevinFlow (see \hyperref[subsubsection:churchland_results_additional]{\ref*{subsubsection:churchland_results_additional}} for full sweep configurations). For each baseline method, we select the model with the highest mean \metric{AUROC} across features using their best matching latents. And to give the baselines a better opportunity to demonstrate both selectivity and discrimination, we report up to two latents per feature -- the highest-\metric{Sel} and highest-\metric{AUROC} dimensions -- alongside the illustrative NLDisco latent. As in \hyperref[subsection:synthetic_results]{\ref*{subsection:synthetic_results}}, this is a targeted search for known features, not a comparison of what each method would discover without specified behavioral descriptions.

As expected, both baselines recover these features. For \textit{recent deceleration}, NLDisco has the highest $\metric{Spec}$ and $\metric{Sel}$, but lower $\metric{Cov}$ than the baselines, while LangevinFlow has the highest $\metric{AUROC}$. For \textit{fast leftward target reach}, NLDisco covers nearly all condition-positive examples with much higher $\metric{Cov}$ than the baselines. All shown latents are highly selective for this feature, with NLDisco and LangevinFlow latents also highly discriminative. These comparisons highlight why the metrics should be considered together: a highly selective latent need not detect most condition-positive examples or discriminate well across activation thresholds, as ROC curves further illustrate (\autoref{figure:churchland_rocs}).

Last, we assess feature decoding from each displayed latent and the full latent space using identical $\ell_2$-regularized logistic regression. We report test balanced accuracy with validation-selected regularization and decision thresholds, using a 0.8/0.1/0.1 whole-trial split (\autoref{table:churchland_comparison}; shared protocol in \hyperref[paragraph:latent_decoding]{\ref*{subsubsection:latents_evaluation_details}}). We see all methods perform well on feature decoding from the full latent space, while single-latent decoding favors NLDisco and LangevinFlow. Overall, we find that CEBRA and LangevinFlow contain recoverable behavioral information, but their objectives do not explicitly favor individual feature detectors. NLDisco makes these a natural starting point for discovery: inspect activating examples, formulate behavioral interpretations, and quantify selectivity and discrimination. Although our comparison does not directly measure end-to-end discovery effort, it shows that NLDisco supports this direct, latent-centered workflow while retaining strong decoding performance. We compare training times for all three methods in \autoref{figure:training_time_h100}.

\begin{table}[tb]
    \centering
    {\setlength{\tabcolsep}{3pt}\resizebox{\linewidth}{!}{\input{figures/churchland_comparison_main.tex}}}
    \caption{\textbf{Feature recovery and decoding.} \metric{Cov}, \metric{Spec}, \metric{Sel}, \metric{AUROC} and feature decoding scores for features and latents shown in \autoref{fig:churchland_results}. ``Single decode'' uses individual latents, while ``All decode'' uses the entire latent space. \metric{Decode} reports test balanced accuracy from logistic regression with validation-selected decision thresholds and a 0.8/0.1/0.1 whole-trial split (\hyperref[paragraph:latent_decoding]{\ref*{subsubsection:latents_evaluation_details}}). Column maxima are bolded, including ties.}
    \label{table:churchland_comparison}
\end{table}
