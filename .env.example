# Copy to .env; existing environment variables take precedence.
# Credentials belong here or in your shell, never in Hydra YAML/CLI overrides.
WANDB_API_KEY=
# Optional W&B server override:
# WANDB_BASE_URL=https://api.wandb.ai
# Data paths can be referenced in YAML as ${oc.env:NLDISCO_DATA}.
# NLDISCO_DATA=/absolute/path/to/counts.npy
